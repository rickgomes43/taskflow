# ============================================================================
# TASKFLOW - PROJECT MANAGEMENT PLATFORM
# ============================================================================
# Configuration template for TaskFlow development environment
# Copy this file to .env and update the values as needed
#
# For detailed setup instructions, see: docs/setup.md
# ============================================================================

# Application Settings
APP_NAME="TaskFlow"
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
APP_TIMEZONE="America/Sao_Paulo"

# Localization
APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=pt_BR

# Application Maintenance
APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP Configuration
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# ============================================================================
# DATABASE CONFIGURATION - DOCKER/SAIL SETUP
# ============================================================================
# TaskFlow uses MySQL with Laravel Sail for development
# The values below are pre-configured for the Docker environment

DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=taskflow
DB_USERNAME=sail
DB_PASSWORD=password

# Alternative SQLite configuration (for testing)
# DB_CONNECTION=sqlite
# DB_DATABASE=database/database.sqlite

# ============================================================================
# REDIS CONFIGURATION - CACHE & SESSIONS
# ============================================================================
# Redis is used for cache, sessions and queues in the Docker environment

CACHE_STORE=redis
CACHE_PREFIX=taskflow_cache

SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# ============================================================================
# QUEUE CONFIGURATION
# ============================================================================
# For background jobs (email sending, report generation, etc.)

QUEUE_CONNECTION=redis
BROADCAST_CONNECTION=log

# ============================================================================
# FILE STORAGE
# ============================================================================
# Configuration for file uploads and storage

FILESYSTEM_DISK=local
# For production, consider using S3:
# FILESYSTEM_DISK=s3

# ============================================================================
# EMAIL CONFIGURATION
# ============================================================================
# Email settings for notifications and user communication

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@taskflow.local"
MAIL_FROM_NAME="${APP_NAME}"

# Production email settings (uncomment and configure for production):
# MAIL_MAILER=smtp
# MAIL_HOST=your-smtp-server.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@domain.com
# MAIL_PASSWORD=your-email-password
# MAIL_ENCRYPTION=tls

# ============================================================================
# AWS/S3 CONFIGURATION (Optional - for production file storage)
# ============================================================================
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# ============================================================================
# TASKFLOW SPECIFIC SETTINGS
# ============================================================================
# Custom configuration for TaskFlow features

# Time tracking settings
TASKFLOW_MAX_DAILY_HOURS=12
TASKFLOW_BREAK_REMINDER_MINUTES=60
TASKFLOW_AUTO_PAUSE_MINUTES=480

# File upload limits
TASKFLOW_MAX_FILE_SIZE=10240
TASKFLOW_ALLOWED_FILE_TYPES="jpg,jpeg,png,gif,pdf,doc,docx,xls,xlsx,zip,rar"

# Client access settings
TASKFLOW_CLIENT_TOKEN_EXPIRY_DAYS=30
TASKFLOW_GUEST_ACCESS_ENABLED=true

# Notification settings
TASKFLOW_SEND_EMAIL_NOTIFICATIONS=true
TASKFLOW_NOTIFICATION_DELAY_MINUTES=5

# ============================================================================
# DEVELOPMENT/DEBUGGING TOOLS
# ============================================================================
# Settings for development environment

VITE_APP_NAME="${APP_NAME}"

# Laravel Telescope (uncomment if installed)
# TELESCOPE_ENABLED=true

# Laravel Debugbar (uncomment if installed)
# DEBUGBAR_ENABLED=true

# ============================================================================
# SOCIAL LOGIN CONFIGURATION (Future Implementation)
# ============================================================================
# OAuth settings for Google login integration

# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URL="${APP_URL}/auth/google/callback"

# ============================================================================
# DOCKER/SAIL PORT CONFIGURATION
# ============================================================================
# Customize ports if there are conflicts on your system

APP_PORT=80
VITE_PORT=5173
FORWARD_DB_PORT=3306
FORWARD_REDIS_PORT=6379

# Alternative ports (uncomment if needed):
# APP_PORT=8080
# FORWARD_DB_PORT=3307
# FORWARD_REDIS_PORT=6380

# ============================================================================
# SAIL CONFIGURATION
# ============================================================================
# Laravel Sail specific settings

SAIL_XDEBUG_MODE=develop,debug,coverage
